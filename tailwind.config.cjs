const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			colors: {
				'primary': '#10141A',
				'accent': '#1C4F9C'
			}
		}
	},

	plugins: [],
};

module.exports = config;
